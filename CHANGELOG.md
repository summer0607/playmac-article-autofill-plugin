# 更新记录

## 3.1.9

- 固定正文严格采用用户提供的样稿：保留标题、红字、提示框、折叠问题和原有换行，恢复相关说明。
- 移除整篇 HTML 区块及整段强制换行样式，仅保护 Steam“关于游戏”的原始结构，不影响其他正文正常分段。
- 真实编辑器保存测试同时比对固定正文排版与 Steam 原文，并验证视频播放；非正文字段和千帆去重逻辑保持不变。
- 需要同时更新插件和常驻组件，保留现有会话与数据卷。

## 3.1.8

- 曾以整段强制换行样式修复固定正文；该处理已在 3.1.9 撤回，改为按样稿正常排版。

## 3.1.7

- 修复后台可视化编辑器再次保存时改写 Steam 原文、移除动态图备用源的问题。
- 编辑器中的“关于游戏”按原文保护；保存和自动保存时恢复完整源内容，其他段落仍可编辑，前端正常显示。
- 增加真实 WordPress 可视化编辑器保存回归测试；本次只需更新插件，兼容已部署的 3.1.6 常驻组件。

## 3.1.6

- 保留 Steam 按浏览器返回的 `picture` 和 `srcset`，防止动态 WebP 被错误降级为静态封面。
- 扩展 WordPress 保存规则和真实页面测试，验证备用图片源与原始标签结构。

## 3.1.5

- “关于游戏”完整保留 Steam“关于本游戏”原文、标题级别、段落、换行和列表，取消 3000 字限制及标题重排。
- 原文图片、动态图和视频直接使用 Steam 链接；封面和五张独立截图继续使用千帆。
- 原文视频在文章前端静音自动播放、隐藏控件并禁止右键，不影响其他视频。
- 保存时保留视频源和安全属性，避免 WordPress 自动改变原文段落；缺少原文则停止导入。
- 需要同时更新 WordPress 插件和服务器常驻组件，现有会话和数据卷保持不变。

## 3.1.4

- “关于游戏”保留原文段落换行，并把主标题、子标题分别转换为三级、四级标题。
- 游戏文章不再勾选一级“Mac游戏”，只勾选匹配的二级或三级分类。
- 分类匹配失败时也只使用二级或三级兜底分类，不再回退到一级分类。

## 3.1.3

- Steam 游戏固定获取五张商店截图，下载后上传千帆，并把五张千帆外链插入“游戏截图”。
- 商店介绍统一转换为安全正文，避免介绍内容截断后导致游戏截图无法显示。
- 资源语言只读取 Steam 商店语言表中的“字幕”列，不再混入音频语言。

## 3.1.2

- 图片上传前按实际文件内容去重，同一批任务不再重复上传相同图片。
- 持久保存图片内容指纹与千帆外链；后续文章再次遇到同一图片时直接复用已有外链。
- 千帆图片使用内容指纹命名；同时兼容查找旧版文件名，避免升级后把已上传图片再传一次。

## 3.1.1

- 游戏正文统一为正式站格式：游戏简介、发行日期、经验建议、注意事项、安装方法、常见问题、关于游戏、游戏截图。
- 移除正文中的独立“配置要求”段落，系统要求仍保留在文章高级配置中。
- Steam 缺少详细介绍或截图时保留对应栏目并显示待补充提示。

## 3.1.0

- 新增千帆官方二维码登录，插件设置页可直接获取二维码并显示扫码状态。
- 扫码确认后由服务器组件保存登录会话，不读取也不保存账号密码。
- 账号密码登录保留为备用方式。

## 3.0.2

- 兼容千帆账号登录页新增的用户协议勾选项。
- 兼容“登 录”按钮的新文字格式，确保邮箱账号会被真正提交。
- 登录失败时返回账号密码或安全验证的实际原因。

## 3.0.1

- 修复千帆默认显示短信登录时，邮箱被错误填入登录方式选择框的问题。
- 登录时自动切换到账号登录并精准填写邮箱和密码。
- 新登录验证失败时不再覆盖已有可用会话。

## 3.0.0

- 改为服务器常驻容器，不再由 WordPress 临时安装或启动 Python。
- 千帆登录会话和文章任务独立保存，插件更新后无需重新初始化。
- 文章处理改为后台异步任务，关闭页面后仍会继续，重新打开可恢复进度。
- GitHub 发布版本时同步生成 WordPress 安装包和服务器容器镜像。

## 2.0.4

- 修复宝塔网站进程无法确定 Python 解释器路径导致初始化失败的问题。
- 初始化时自动解析 Python 绝对路径，并检查最低版本后创建独立运行环境。

## 2.0.3

- 修复插件页“检查 GitHub 更新”按钮未清除旧缓存的问题。
- GitHub 发布新版本后，WordPress 插件页会按标准流程显示并安装更新。

## 2.0.2

- 修复 GitHub 更新检查缓存旧版本导致新版本未及时显示的问题。

## 2.0.1

- 修复服务器禁用进程功能时保存千帆账号会触发网站严重错误的问题，改为设置页内提示。
- 在设置页增加“测试千帆连接”按钮，可确认当前登录是否有效。

## 2.0.0

- 正式站独立运行，不再调用本机控制台。
- 插件内完成 Steam、Macked、千帆图片处理和草稿保存。
- 增加 GitHub Release 自动更新检查。
